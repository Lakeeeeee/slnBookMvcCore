@inject Microsoft.Extensions.Configuration.IConfiguration Configration

<script src="https://www.google.com/recaptcha/api.js?render=@Configration["googleConfiguration:siteKey"]"></script>
<script>
    grecaptcha.ready(function () {
        window.grecaptcha.execute('@Configration["googleConfiguration:siteKey"]', { action: 'submit' }).then(function (token) {
            $('#captchaInput').val(token);
        });
    });
            }
</script>