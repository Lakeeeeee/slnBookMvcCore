@{
    ViewData["Title"] = "Menu";
    Layout = "~/Views/Shared/_Layout_MENU.cshtml";
}
<style>
    /*
                    /*    div {
                            border: 1px solid #ff0000;
                        }*/
    td {
        padding: 0 20px;
    }

    button {
        border: 1px solid black;
        border-radius: 20px;
    }
</style>
<div>
    <div class="container body-content">
        <div class="row">
            <div class="col align-content-start" style="display: flex">
                <h3>推薦文章</h3>
            </div>
            <div class="col justify-content-end" style="display: flex; padding:5px">
                <a role="button" class="btn btn-sm btn-outline-success mx-1" href="~/Artical/List全部文章">看更多</a>
            </div>
        </div>
        <div class="row">
            <div id="carouselExampleNew" class="carousel slide">
                <div class="carousel-inner">
                    @*輪播推薦文章*@
                    <div class="carousel-item active">
                        @*輪播書本第一頁*@
                        <div class="row">
                            @{
                                BookShopContext db = new BookShopContext();
                                int latestArticalId = 0;
                                var Artical = db.Articals.OrderByDescending(a => a.ArticalId).Select(a => a).Take(1);
                                foreach (var g in Artical)
                                {
                                    string path = g.ArticalTitle + ".jpg";

                                    latestArticalId = g.ArticalId;
                                    <table>
                                        <tr>
                                            <td colspan="2">
                                                <div style="text-align:center"><a href="~/Artical/Detail文章/@g.ArticalId"> <img src="~/Image/ArticalPicture/@path" height="300" title="@g.ArticalTitle" /> </a></div>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td><span> @g.ArticalTitle</span></td>
                                            <td style="color: gray; font-style: oblique;font-size: 60%;text-align: right;"><span>更新日期：@g.ArticalDate</span> </td>
                                        </tr>
                                    </table>
                                }
                                #region 文章內書目
                                //    var detail = db.ArticalToBookDetails.Where(d => d.ArticalId == 1).Select(d => new { d.Book.BookTitle, d.Book.UnitPrice, d.Book.CoverPath, d.Book.BookId, }).ToList();
                                //                   <table>
                                //                   <tr>
                                //                    @foreach (var b in detail)
                                //    {
                                //                string title中 = b.BookTitle.Split("$")[0];
                                //                var q作者 = db.AuthorDetails.Where(a => a.BookId == b.BookId).Select(a => new { a.Author.AuthorName }).ToList();
                                //                string s作者 = "";
                                //                foreach (var item in q作者) { s作者 += item.AuthorName + "    "; }
                                //                string price原價 = $"{b.UnitPrice:c0}";

                                //                        <td>
                                //                            <div style="position:relative">
                                //                                <a id="@b.BookId"><img src="@b.CoverPath" class="d-block w-100" alt="..." @b.CoverPath /></a>
                                //                            </div>
                                //                            <div class="caption bookCaption">
                                //                                <ul style="list-style:none; margin:0; padding:0;">
                                //                                    <li class='prod_TitleMain'> <a id="@b.BookId"> @title中</a> </li>
                                //                            <li class="discAuthor">作者：@s作者</li>
                                //                            <li class="discPrice原價">原價：<del style="color:red;font-style:oblique">@price原價</del></li>
                                //                                 @{
                                //    var bookDiscountId = db.BookDiscountDetails.Where(a => a.BookId == b.BookId).Select(a => a.BookDiscountId).FirstOrDefault();
                                //    var q是否優惠 = bookDiscountId != null ? bookDiscountId.ToString() : "";
                                //    if (!string.IsNullOrEmpty(q是否優惠))
                                //    {
                                //        var q優惠折數 = db.BookDiscounts.Where(a => a.BookDiscountId == int.Parse(q是否優惠)).Select(a => new { a.BookDiscountName, a.BookDiscountAmount }).FirstOrDefault();
                                //        var price優惠價 = $"{(q優惠折數.BookDiscountAmount* b.UnitPrice):0}";
                                //                                    <li class="discPrice優惠價">優惠價：@q優惠折數.BookDiscountName @price優惠價 元</li>
                                //}    }
                                //                                </ul>
                                //                            </div>
                                //                        </td>
                                //    }
                                //                        </tr>
                                //                    </table>
                                #endregion

                            }
                        </div>
                    </div>
                    @*輪播書本第二~四頁*@
                    @{
                        for (int i = 2; i <= 4; i++)
                        {
                            <div class="carousel-item">
                                <div class="row">
                                    @{
                                        db = new BookShopContext();
                                        int preArticalId = latestArticalId - i;
                                        Artical = db.Articals.Where(a => a.ArticalId == preArticalId).Select(a => a);
                                        foreach (var g in Artical)
                                        {
                                            string path = g.ArticalTitle + ".jpg";

                                            <table>
                                                <tr>
                                                    <td colspan="2">
                                                        <div style="text-align:center"><a href="~/Artical/Detail文章/@g.ArticalId"> <img src="~/Image/ArticalPicture/@path" height="300" title="@g.ArticalTitle" /> </a></div>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td>@g.ArticalTitle</td>
                                                    <td style="color: gray; font-style: oblique;font-size: 60%;text-align: right;">更新日期：@g.ArticalDate</td>
                                                </tr>
                                            </table>
                                        }
                                        #region 文章內書目
                                        //    detail = db.ArticalToBookDetails.Where(d => d.ArticalId == i).Select(d => new { d.Book.BookTitle, d.Book.UnitPrice, d.Book.CoverPath, d.Book.BookId, }).ToList();
                                        //                <table>
                                        //                    <tr>
                                        //                        @foreach (var b in detail)
                                        // {
                                        //                string title中 = b.BookTitle.Split("$")[0];
                                        //                var q作者 = db.AuthorDetails.Where(a => a.BookId == b.BookId).Select(a => new { a.Author.AuthorName }).ToList();
                                        //                string s作者 = "";
                                        //                foreach (var item in q作者) { s作者 += item.AuthorName + "    "; }
                                        //                string price原價 = $"{b.UnitPrice:c0}";

                                        //                <td>
                                        //                    <div style="position:relative">
                                        //                        <a id="@b.BookId"><img src="@b.CoverPath" class="d-block w-100" alt="..." @b.CoverPath /></a>
                                        //                    </div>
                                        //                    <div class="caption bookCaption">
                                        //                        <ul style="list-style:none; margin:0; padding:0;">
                                        //                            <li class='prod_TitleMain'> <a id="@b.BookId"> @title中</a> </li>
                                        //                            <li class="discAuthor">作者：@s作者</li>
                                        //                            <li class="discPrice原價">原價：<del style="color:red;font-style:oblique">@price原價</del></li>
                                        //                            @{
                                        //                                var bookDiscountId = db.BookDiscountDetails.Where(a => a.BookId == b.BookId).Select(a => a.BookDiscountId).FirstOrDefault();
                                        //                                var q是否優惠 = bookDiscountId != null ? bookDiscountId.ToString() : "";
                                        //                                if (!string.IsNullOrEmpty(q是否優惠))
                                        //                                {
                                        //                                    var q優惠折數 = db.BookDiscounts.Where(a => a.BookDiscountId == int.Parse(q是否優惠)).Select(a => new { a.BookDiscountName, a.BookDiscountAmount }).FirstOrDefault();
                                        //                                    var price優惠價 = $"{(q優惠折數.BookDiscountAmount * b.UnitPrice):0}";
                                        //                                    <li class="discPrice優惠價">優惠價：@q優惠折數.BookDiscountName @price優惠價 元</li>
                                        //                                }
                                        //                            }
                                        //                        </ul>
                                        //                                </div>
                                        //                            </td>
                                        //}
                                        //                    </tr>
                                        //                </table>
                                        #endregion
                                    }
                                </div>

                            </div>
                        }
                    }
                    @*輪播圖切換按鈕*@ <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleNew" data-bs-slide="prev" border=5>
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    @*輪播圖切換按鈕*@<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleNew" data-bs-slide="next" border=5>
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>
        </div>

        <br />
        <hr />
        <br />
        <div class="row">
            <div class="col align-content-start" style="display: flex">
                <h3>中文書籍</h3>
            </div>
            <div class="col justify-content-end" style="display: flex; padding:5px">
                <a role="button" class="btn btn-sm btn-outline-success mx-1" href="~/Category/中文書">看更多</a>
            </div>
            <div class="container body-content">
                <div id="carouselExampleEdit" class="carousel slide">
                    <div class="carousel-inner">
                        <div class="carousel-item active">
                            @*輪播書本第一頁*@
                            <div class="row">
                                @{
                                    int latestBookId = 0;
                                    var Books = db.Books.OrderByDescending(a => a.BookId).Select(a => a).Take(1);
                                    foreach (var categoryBook in Books)
                                    {
                                        var book = db.Books.Where(b => b.BookId == categoryBook.BookId).FirstOrDefault();
                                        var author = db.AuthorDetails.Where(a => a.BookId == categoryBook.BookId).Select(a => new { a.AuthorId }).FirstOrDefault();
                                        var authorName = author != null ? db.Authors.Where(a => a.AuthorId == author.AuthorId).Select(a => new { a.AuthorName }).FirstOrDefault() : null;
                                        var discount = db.BookDiscountDetails.Where(d => d.BookId == categoryBook.BookId).Select(d => new { d.BookDiscountId }).FirstOrDefault();
                                        var discountAmount = discount != null ? db.BookDiscounts.Where(d => d.BookDiscountId == discount.BookDiscountId).Select(d => new { d.BookDiscountId, d.BookDiscountAmount, d.BookDiscountName }).FirstOrDefault() : null;
                                        var discountedPrice = book?.UnitPrice * (1 - (discountAmount != null ? discountAmount.BookDiscountAmount : 0) / 100) ?? 0;
                                        latestBookId = categoryBook.BookId;
                                        <div class="col book-item" data-publication-date="@book?.PublicationDate" data-price="@((int)Math.Round(discountedPrice))">
                                            <div style="position:relative">
                                                <a><img src="@book?.CoverPath"></a>
                                            </div>
                                            <div class="caption">
                                                <ul style="list-style:none; margin:0; padding:0;">
                                                    <li>
                                                        <a class="bookCaption">@book?.BookTitle</a>
                                                    </li>
                                                    <li class='prod_author'>
                                                        作者：@if (authorName != null)
                                                        {
                                                            <a href="/rwd_searchResult.html?keyword%5B%5D=@authorName&keyType%5B%5D=2">@authorName.AuthorName</a>
                                                        }
                                                    </li>
                                                    <li class="Price">原    價：<span>@((int)Math.Round(book?.UnitPrice ?? 0))</span>元</li>
                                                    <li class="discPrice">優惠價：<span>@(discountAmount != null ? discountAmount.BookDiscountName : "")</span><span>@((int)Math.Round(discountedPrice))</span>元</li>
                                                    <li class="publicationDate">出版日期：<span>@book?.PublicationDate.ToString("yyyy/MM/dd")</span></li>
                                                </ul>
                                            </div>
                                        </div>
                                    }
                                }
                            </div>
                        </div>
                        @*輪播書本第二~四頁*@
                        @{
                            for (int i = 2; i <= 4; i++)
                            {
                                <div class="carousel-item">
                                    <div class="row">
                                        @{
                                            int preBookId = latestBookId - i;
                                            Books = db.Books.OrderByDescending(a => a.BookId).Select(a => a);
                                            foreach (var categoryBook in Books)
                                            {
                                                var book = db.Books.Where(b => b.BookId == categoryBook.BookId).FirstOrDefault();
                                                var author = db.AuthorDetails.Where(a => a.BookId == categoryBook.BookId).Select(a => new { a.AuthorId }).FirstOrDefault();
                                                var authorName = author != null ? db.Authors.Where(a => a.AuthorId == author.AuthorId).Select(a => new { a.AuthorName }).FirstOrDefault() : null;
                                                var discount = db.BookDiscountDetails.Where(d => d.BookId == categoryBook.BookId).Select(d => new { d.BookDiscountId }).FirstOrDefault();
                                                var discountAmount = discount != null ? db.BookDiscounts.Where(d => d.BookDiscountId == discount.BookDiscountId).Select(d => new { d.BookDiscountId, d.BookDiscountAmount, d.BookDiscountName }).FirstOrDefault() : null;
                                                var discountedPrice = book?.UnitPrice * (1 - (discountAmount != null ? discountAmount.BookDiscountAmount : 0) / 100) ?? 0;
                                                latestBookId = categoryBook.BookId;
                                                <div class="col book-item" data-publication-date="@book?.PublicationDate" data-price="@((int)Math.Round(discountedPrice))">
                                                    <div style="position:relative">
                                                        <a><img src="@book?.CoverPath"></a>
                                                    </div>
                                                    <div class="caption">
                                                        <ul style="list-style:none; margin:0; padding:0;">
                                                            <li>
                                                                <a class="bookCaption">@book?.BookTitle</a>
                                                            </li>
                                                            <li class='prod_author'>
                                                                作者：@if (authorName != null)
                                                                {
                                                                    <a href="/rwd_searchResult.html?keyword%5B%5D=@authorName&keyType%5B%5D=2">@authorName.AuthorName</a>
                                                                }
                                                            </li>
                                                            <li class="Price">原    價：<span>@((int)Math.Round(book?.UnitPrice ?? 0))</span>元</li>
                                                            <li class="discPrice">優惠價：<span>@(discountAmount != null ? discountAmount.BookDiscountName : "")</span><span>@((int)Math.Round(discountedPrice))</span>元</li>
                                                            <li class="publicationDate">出版日期：<span>@book?.PublicationDate.ToString("yyyy/MM/dd")</span></li>
                                                        </ul>
                                                    </div>
                                                </div>
                                            }
                                        }
                                    </div>
                                </div>
                            }
                        }

                        @*輪播圖切換按鈕*@ <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleEdit" data-bs-slide="prev" border=5>
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Previous</span>
                        </button>
                        @*輪播圖切換按鈕*@<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleEdit" data-bs-slide="next" border=5>
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Next</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


